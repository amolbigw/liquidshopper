@AGENTS.md

# Git & Deploy Workflow
After every commit to `feature/automotive-platform`, always also merge it into `main` and push both branches. This ensures every change triggers a production Vercel deployment automatically. Do not ask for confirmation — just do it.
